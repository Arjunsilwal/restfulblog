<% include partials/header %>

<% blogs.forEach( blog =>{ %>
  <div class="">
    <h2><%= blog.title %></h2>
   <img src="<%= blog.image %>">
   <span><%= blog.created %></span>
   <p><%= blog.body %></p>
  </div>

<% }); %>
